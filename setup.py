from setuptools import setup

setup(
    name='int_privileges',
    version='0.1',
    description='Пакет для работы с иерархическим представлением привилегий',
    url='https://github.com/vadikko2/privileges',
    install_requires=[],
    packages=['privileges'],
)
